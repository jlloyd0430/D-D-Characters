<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Yeon+Sung&display=swap" rel="stylesheet">

  <title>
    <%= Character.name %>
  </title>
  <style>
     body {
      font-family: Arial, sans-serif;
      background-color: black;
      font-family: 'Yeon Sung', cursive;
    }

    a {
      text-decoration: none;
      color: #0000ff;
      margin: 15px;
    }

    h1 {
      font-size: 36px;
      text-align: center;
      color: #6439b9;
      margin: 10px;
    }

    h2 {
      font-size: 24px;
      color: #666666;
      margin: 10px;
    }

    .character {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
    }

    .character-card {
      margin: auto;
      border-radius: 11px;
      width: 300px;
      height: 400px;
      background-color: #fff;
      box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
      margin: 20px;
      overflow: hidden;
    }

    .character-card img {
      width: 100%;
      height: 200px;
      object-fit: cover;
    }

    .character-card h3 {
      font-size: 18px;
      text-align: center;
      margin: 10px;
    }

    .character-card p {
      font-size: 14px;
      margin: 10px;
    }

    .character-card button {
      background-color: #ff0000;
      color: #ffffff;
      font-size: 14px;
      padding: 8px 16px;
      border: none;
      cursor: pointer;
      margin: 10px;
    }

    .character-card button:hover {
      background-color: #ff6666;
    }

    input {
      text-align: center;
      margin: 15px;
    }

   
  </style>
</head>

<body>
  <!-- Takes clients back to the entire collection -->
  <a href="/allChars">Back to all Characters</a>

  <div class="character-card">
    <h1>
      ID # <%= Character.id %>
    </h1>
    <h2>
      Name: <%= Character.name %>
    </h2>
    <h3>
      Class: <%= Character.class %>
    </h3>
    <h3>
      Level: <%= Character.level %>
    </h3>
      <h3>
        Race: <%= Character.race %>
      </h3>
    
  

    <a href="/updateChar/<%=Character.name%>" style="margin: auto;">Update This Character</a>

    <!-- When submitted, this sends the name to the back-end


     When submitted, this sends the name to the back-end to delete THIS document from the collection -->
    <form action="/api/deleteOneCharacter/<%=Character.name%>?_method=DELETE" method="post" style="margin-top: 15px;">
      <input type="submit" name="_method" value="DELETE THIS CHARACTER">
    </form>
</body>

</html>